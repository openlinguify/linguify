# === DÉVELOPPEMENT LOCAL ===
# Django Core Settings
SECRET_KEY="générez-une-nouvelle-clé-secrète"
DEBUG=True
DJANGO_ENV="development"

# URLs Configuration
BACKEND_URL="http://localhost:8000"
FRONTEND_URL="http://localhost:3000"
ALLOWED_HOSTS="localhost,127.0.0.1"

# Database Configuration (Docker)
DB_NAME="db_linguify_dev"
DB_USER="postgres"
DB_PASSWORD="changez-ce-mot-de-passe"
DB_HOST="localhost"
DB_PORT="5432"

# Auth0 Configuration (Créez votre propre compte Auth0)
AUTH0_DOMAIN="votre-domaine-auth0"
AUTH0_CLIENT_ID="votre-client-id"
AUTH0_CLIENT_SECRET="votre-client-secret"
AUTH0_AUDIENCE="votre-audience"
FRONTEND_CALLBACK_URL="http://localhost:3000/callback"

# Email Configuration (Optionnel pour dev)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USE_TLS=True
EMAIL_HOST_USER="votre-email@gmail.com"
EMAIL_HOST_PASSWORD="votre-mot-de-passe-app"
DEFAULT_FROM_EMAIL="Linguify <no-reply@linguify.com>"

# Security Settings
CORS_ALLOWED_ORIGINS="http://localhost:3000"
CSRF_TRUSTED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Redis Configuration (Docker)
REDIS_URL="redis://127.0.0.1:6379/1"

# AI Provider (pour développement)
AI_PROVIDER="simulator"