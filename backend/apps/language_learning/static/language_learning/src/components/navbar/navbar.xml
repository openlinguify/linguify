<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="language_learning.Navbar">
    <nav class="navbar navbar-expand-lg bg-primary text-white mb-4">
        <div class="container-fluid">
            <!-- Brand -->
            <div class="navbar-brand text-white">
                <i class="bi bi-translate me-2"></i>
                Language Learning
            </div>

            <!-- Language selector and streak -->
            <div class="d-flex align-items-center">
                <!-- Current language -->
                <div class="me-4">
                    <span class="badge bg-light text-dark me-2" t-esc="languageFlag"/>
                    <span class="text-white fw-bold" t-esc="props.selectedLanguageName"/>
                </div>

                <!-- Streak indicator -->
                <div class="me-3">
                    <span class="badge bg-warning text-dark">
                        <i class="bi bi-fire me-1"></i>
                        <t t-esc="props.userStreak"/> jours
                    </span>
                </div>

                <!-- Language dropdown -->
                <div class="dropdown">
                    <button class="btn btn-outline-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                        <i class="bi bi-globe me-1"></i>
                        Changer de langue
                    </button>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="#" t-on-click="() => this.onLanguageSelect('EN')">
                                🇬🇧 English
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="#" t-on-click="() => this.onLanguageSelect('FR')">
                                🇫🇷 Français
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="#" t-on-click="() => this.onLanguageSelect('ES')">
                                🇪🇸 Español
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
</t>

</templates>