<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="language_learning.UnitCard">
    <div t-att-class="cardClass" t-on-click="onClick">
        <div class="d-flex justify-content-between align-items-start mb-2">
            <h6 class="unit-title mb-1">
                <span class="badge bg-primary me-2">Unité <t t-esc="props.unit.unit_number"/></span>
                <t t-esc="props.unit.title"/>
            </h6>
            <t t-if="props.unit.progress_percentage === 100">
                <i class="bi bi-check-circle-fill text-success fs-5"></i>
            </t>
        </div>

        <p class="text-muted small mb-3" t-esc="props.unit.description"/>

        <!-- Progress bar -->
        <div class="progress mb-2" style="height: 8px;">
            <div
                t-att-class="`progress-bar ${progressClass}`"
                role="progressbar"
                t-att-style="`width: ${progressWidth}`"
                t-att-aria-valuenow="props.unit.progress_percentage"
                aria-valuemin="0"
                aria-valuemax="100"
            ></div>
        </div>

        <!-- Stats -->
        <div class="d-flex justify-content-between align-items-center">
            <small class="text-muted">
                <i class="bi bi-list-check me-1"></i>
                <t t-esc="props.unit.completed_modules"/>/<t t-esc="props.unit.modules_count"/> modules
            </small>
            <small t-att-class="`badge ${progressClass === 'bg-success' ? 'bg-success' : 'bg-secondary'}`">
                <t t-esc="props.unit.progress_percentage"/>%
            </small>
        </div>
    </div>
</t>

</templates>