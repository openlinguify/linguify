"use client";

// Placeholder component for flip card content
// The original component had dependencies on external packages that are not available

export default function FlipCardContent() {
  return (
    <div className="p-4 bg-white dark:bg-gray-800 rounded-lg border">
      <p className="text-center text-gray-600 dark:text-gray-400">
        Flip card content placeholder
      </p>
    </div>
  );
}