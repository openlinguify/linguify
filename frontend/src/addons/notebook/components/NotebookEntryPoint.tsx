import React from 'react';
import NotebookMain from './NotebookMain';

/**
 * Composant d'entrée pour l'application Notebook
 * Ce composant sert de point d'entrée pour l'application
 */
const NotebookEntryPoint = () => {
  return <NotebookMain />;
};

export default NotebookEntryPoint;